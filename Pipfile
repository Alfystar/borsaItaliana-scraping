[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
httpx = ">=0.27"
beautifulsoup4 = ">=4.12"
lxml = ">=5.0"

[dev-packages]
pytest = ">=8.0"
black = ">=24.0"
ruff = ">=0.4"

[requires]
python_version = "3.11"
